# Contributing

Run `pytest` and `ruff check .` before opening a pull request. Connector changes should use mocked network responses in tests and must not commit downloaded feeds, repository caches, credentials, or proprietary vulnerability data.
